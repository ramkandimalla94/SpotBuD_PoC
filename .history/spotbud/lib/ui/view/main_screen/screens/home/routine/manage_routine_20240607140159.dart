import 'package:flutter/material.dart';

class ManageRoutine extends StatelessWidget {
  const ManageRoutine({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
