import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color.fromARGB(255, 0, 0, 0);
  static const Color secondaryColor = Color.fromRGBO(192, 192, 192, 1);

  static const Color acccentColor = Color.fromRGBO(255, 215, 0, 1);

  static const Color lightacccentColor = Color.fromRGBO(255, 217, 0, 0.549);

  static const Color backgroundColor = Color.fromARGB(255, 255, 255, 255);

  static const Color black = Colors.black;

  // Add more color constants as needed
}
